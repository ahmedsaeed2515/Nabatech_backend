# ⚠️ Missing / Placeholder Environment Variables
> Generated by TASK-0.5 — Must be resolved before production deployment

| Variable | Current Value | Required For | Action |
|----------|--------------|-------------|--------|
| `OPENAI_API_KEY` | `sk-proj-xxxxx` (placeholder) | Agent Tool Calling | Replace with real key from platform.openai.com |
| `FIREBASE_CREDENTIALS` | missing / empty | Push Notifications (FCM) | Add full service account JSON |
| `RAG_API_KEY` | verify in .env | Knowledge Base RAG | Confirm real key is set |
| `OPENWEATHERMAP_API_KEY` | verify in .env | Weather tool | Confirm real key is set |

## Security Notes
- MongoDB Atlas credentials are stored in `.env` — move to AWS Secrets Manager or equivalent before production
- JWT secrets must be rotated before production
- `AI_SECRETS_ENCRYPTION_KEY` must not be in version control
- Remove `test_controller.ts` from production (see Task 5.4)
